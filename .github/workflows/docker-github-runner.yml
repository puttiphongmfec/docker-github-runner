name: Package
on:
  workflow_dispatch:
  release:
    types: [created]
jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    
    - name: build packages
      run: echo "Hello, World!"
